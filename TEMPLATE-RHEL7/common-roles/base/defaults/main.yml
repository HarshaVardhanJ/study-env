---
hostname: unnamed.example.com

required_packages:
  - vim
  - chrony
  - bash-completion
  - selinux-policy-devel
  - libselinux-python

firewall_services:
  - ssh

is_services: false

server_list:
 - No servers are set
 